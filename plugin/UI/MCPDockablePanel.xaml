<Page x:Class="revit_mcp_plugin.UI.MCPDockablePanel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
      Background="#1E1E2E" Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <Border Grid.Row="0" Background="#353550" Padding="8,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Claude" Foreground="White" FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text=" in Revit" Foreground="#8080A0" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- MCP Status -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Ellipse x:Name="StatusIndicator" Width="7" Height="7" Fill="#FF4444" Margin="0,0,4,0"/>
                    <TextBlock x:Name="StatusText" Text="MCP Off" Foreground="#FF6666" FontSize="9" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Navigation buttons -->
                <Button Grid.Column="2" Content="Home" Click="GoHome_Click"
                        Background="#2A2A3C" Foreground="#C0C0D8" BorderThickness="0"
                        Padding="8,3" FontSize="10" Margin="0,0,4,0" Cursor="Hand">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                </Button>
                <Button Grid.Column="3" Content="Reload" Click="Reload_Click"
                        Background="#2A2A3C" Foreground="#C0C0D8" BorderThickness="0"
                        Padding="8,3" FontSize="10" Cursor="Hand">
                    <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                </Button>
            </Grid>
        </Border>

        <!-- WebView2 Browser -->
        <wv2:WebView2 Grid.Row="1" x:Name="WebBrowser"
                      DefaultBackgroundColor="#1E1E2E"/>

        <!-- Loading overlay -->
        <Border Grid.Row="1" x:Name="LoadingOverlay" Background="#1E1E2E" Visibility="Visible">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Loading Claude..." Foreground="#8080A0" FontSize="16" HorizontalAlignment="Center"/>
                <TextBlock Text="Connecting to claude.ai" Foreground="#606080" FontSize="11" HorizontalAlignment="Center" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
